# SETUP

- Create a venv
- Install Requirements
- Run command 'commands/reset_db.bat' to refresh db.
- Run script from 'commands/migrations.bat' to migrate models.
- Run command 'commands/seed.bat' to populate other data
- Run server with 'python manage.py runserver'
